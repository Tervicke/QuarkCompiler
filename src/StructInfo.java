public class StructInfo {
    int slot; // the slot in which struct is saved
    String name; //the type of struct user defined name comes here
    public StructInfo(int slot , String name){
        this.slot = slot;
        this.name = name;
    }
}
